import { KagRoleColorPipe } from './kag-role-color.pipe';

describe('KagRoleColorPipe', () => {
  it('create an instance', () => {
    const pipe = new KagRoleColorPipe();
    expect(pipe).toBeTruthy();
  });
});
