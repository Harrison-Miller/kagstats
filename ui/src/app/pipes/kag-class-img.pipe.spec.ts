import { KagClassImgPipe } from './kag-class-img.pipe';

describe('KagClassImgPipe', () => {
  it('create an instance', () => {
    const pipe = new KagClassImgPipe();
    expect(pipe).toBeTruthy();
  });
});
