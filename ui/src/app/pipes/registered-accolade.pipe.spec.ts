import { RegisteredAccoladePipe } from './registered-accolade.pipe';

describe('RegisteredAccoladePipe', () => {
  it('create an instance', () => {
    const pipe = new RegisteredAccoladePipe();
    expect(pipe).toBeTruthy();
  });
});
