import { HitterIconPipe } from './hitter-icon.pipe';

describe('HitterIconPipe', () => {
  it('create an instance', () => {
    const pipe = new HitterIconPipe();
    expect(pipe).toBeTruthy();
  });
});
