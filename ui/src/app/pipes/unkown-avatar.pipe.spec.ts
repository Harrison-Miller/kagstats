import { UnkownAvatarPipe } from './unkown-avatar.pipe';

describe('UnkownAvatarPipe', () => {
  it('create an instance', () => {
    const pipe = new UnkownAvatarPipe();
    expect(pipe).toBeTruthy();
  });
});
